[package]
name = "netperf-core"
version = "0.1.0"
edition = "2024"
description = "Shared protocol types, percentiles, and reporting for netperf (transport-agnostic)"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
colored = "3.1"
